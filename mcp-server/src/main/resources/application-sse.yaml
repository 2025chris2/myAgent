spring:
  ai:
    mcp:
      server:
        name: llong-image-search-mcp-server
        version: 0.0.1
        enabled: true
        sse-endpoint: /sse
        type: SYNC
        stdio: false

server:
  port: 8127
