spring:
  ai:
    mcp:
      server:
        name: llong-image-search-mcp-server
        version: 0.0.1
        enabled: true
        type: SYNC
        annotation-scanner:
          enabled: true
        capabilities:
          tool: true
        sse-message-endpoint: /mcp/message

server:
  port: 8127
